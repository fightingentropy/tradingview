# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v56.0.0/ before writing any code.

## Local Dev

- `ios-sim-mirror`: run `/Users/erlinhoxha/.sim-mirror/start-fast-sim.zsh`.
- Stop it with `/Users/erlinhoxha/.sim-mirror/stop-fast-sim.zsh`.
- The headless `serve-sim` mirror listens only on `127.0.0.1:3399`; Simulator.app is not launched.
