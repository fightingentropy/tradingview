# Copy to .env.local for development, or .env.production for a production build.
# VITE_ values are public browser configuration. Never put private keys here.
VITE_CONVEX_URL=https://YOUR_DEPLOYMENT.convex.cloud
# Only set when the existing Convex backend also has Turnstile enabled.
# VITE_TURNSTILE_SITE_KEY=
