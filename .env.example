# Real US stock data (Twelve Data) — free key: https://twelvedata.com/pricing
# Server-side only (no EXPO_PUBLIC_ prefix) so it never ships in the app bundle.
TWELVE_DATA_KEY=

# Optional: override the stocks proxy origin (defaults to the Expo dev server).
# EXPO_PUBLIC_API_ORIGIN=
